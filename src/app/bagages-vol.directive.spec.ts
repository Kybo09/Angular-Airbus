import { BagagesVolDirective } from './bagages-vol.directive';

describe('BagagesVolDirective', () => {
  it('should create an instance', () => {
    const directive = new BagagesVolDirective();
    expect(directive).toBeTruthy();
  });
});
